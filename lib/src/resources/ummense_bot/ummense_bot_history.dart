import 'package:flutter/material.dart';

class UmmenseBotHistory extends StatelessWidget {
  const UmmenseBotHistory();

  @override
  Widget build(BuildContext context) {
    return Container(
      child: const Text(''),
    );
  }
}