import 'package:flutter/material.dart';

class UmmenseBotSuccess extends StatelessWidget {
  const UmmenseBotSuccess();

  @override
  Widget build(BuildContext context) {
    return Container(
      child: const Text(''),
    );
  }
}